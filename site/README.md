# TraceMail AI — Professional UI

Open `index.html` in a browser. Keep the bundled forensic PDF in the same folder.

The UI redesign preserves the existing TraceMail AI workflow and adds a clear first-screen choice between linking a mailbox and adding one `.EML` file.
